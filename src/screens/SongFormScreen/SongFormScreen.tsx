import { View, Text } from 'react-native';

export interface IHomeScreenPropTypes {}

export default (_props: IHomeScreenPropTypes) => {
  return (
    <View>
      <Text>Song Form Page</Text>
    </View>
  );
};
