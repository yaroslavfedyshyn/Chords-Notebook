import { HOME_PAGE_ROUTE, SONG_FORM_ROUTE } from './routes';

export type TStackRoutes = {
  [HOME_PAGE_ROUTE]: undefined;
  [SONG_FORM_ROUTE]: undefined;
};

export type TAllStackProps = TStackRoutes;
