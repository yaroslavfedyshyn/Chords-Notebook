import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'relative',
    height: '100%',
    width: '100%',
  },
  fabButtonContainer: {
    position: 'absolute',
    bottom: 10,
    right: 10,
  },
});
